import { Router } from 'express';
import { CreatePayments } from '../controllers/PaymentsControllers/GetProducts.js'

const routes = Router();

routes.post('/api/payments', /* criar controller */);

export default routes;